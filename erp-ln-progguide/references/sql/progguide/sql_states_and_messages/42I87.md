# 42I87 - Invalid text content function
An argument of the text_content function is invalid.

## Example
```

select text_content(text, '12')
from dbtst107
```
Because the language argument is invalid, this query will cause the error:
SQLState 42I87: The length of the language specification in a text_content function must be exactly 1
