# 42T30 - Invalid type for TRIM operand
The argument of the TRIM function does not have type string.

## Example
```

select trim(1)
from dbtst120
```
The TRIM argument is of type integer instead of string. This query will cause the error:
SQLState 42T30: Invalid type for operand of trim operator. Expected type STRING
