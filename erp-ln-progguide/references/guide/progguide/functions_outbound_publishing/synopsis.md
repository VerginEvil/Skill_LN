# Outbound Publishing functions synopsis
```

long
```
```

long
```
```

long
```
```

long
```
```

long
```
| | | |
|---|---|---|
|  | [publish.check()](publish.check.md) | `(const string bus.component.name, ref long result.xml)` |
|  | [publish.open()](publish.open.md) | `(const string bus.component.name, ref long result.xml)` |
|  | [publish.message()](publish.message.md) | `(long publication.id, long message.xml, ref long result.xml)` |
|  | [publish.error.message()](publish.error.message.md) | `(long publication.id, long error.xml, ref long result.xml)` |
|  | [publish.close()](publish.close.md) | `(long publication.id, ref long result.xml)` |

## Related topic
- [Outbound Publishing functions overview](overview.md)
