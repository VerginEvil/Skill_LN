# 42I17 - Cycle in REFERS TO predicates
An SQL statement contains a series of REFERS TO predicates that together define a cyclic relationship between the tables that refer to each other.

## Example 1
```

select *
from   dbtst120, dbtst100
where  dbtst120.workdept refers to dbtst100
and    dbtst100.mgrno refers to dbtst120
```
The tables dbtst120 and dbtst100 refer to each other. This query causes the error:
SQLState 42I17: Cycle in REFERS TO predicates

## Example 2
```

select *
from   dbtst120
where  empno refers to dbtst120
```
Table dbtst120 refers to itself. This query also causes the error:
SQLState 42I17: Cycle in REFERS TO predicates

## Related topics
- [REFERS TO predicate](../functions_database_handling/refers_to_pred.md)
