# 42I81 - Row value constructor in IS NULL
The IS NULL predicate operates on value expressions. It is not allowed to use IS NULL on row value constructors.

## Example
```

select *
from   dbtst120
where  { firstnme } IS NULL
```
This query will cause the error:
SQLState 42I81: Row value constructor in IS NULL predicate
