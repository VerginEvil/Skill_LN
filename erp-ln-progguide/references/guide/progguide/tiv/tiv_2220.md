# TIV 2220

## Enterprise Server 10.6.1
- [http.mimepart.new()](../functions_http_client/http.mimepart.new.md)

- [http.mimepart.delete()](../functions_http_client/http.mimepart.delete.md)

- [http.mimepartlist.new()](../functions_http_client/http.mimepartlist.new.md)

- [http.mimepartlist.delete()](../functions_http_client/http.mimepartlist.delete.md)

- [http.mimepartlist.add()](../functions_http_client/http.mimepartlist.add.md)

- [http.mimepartlist.add_list()](../functions_http_client/http.mimepartlist.add_list.md)

## Porting set 9.2b
- rdi.table.referenced()

- string curl.version()

- long curl.mime.init ()

- long curl.mime.addpart (long mime_handle)

- long curl.mime.name (long mime_handle, const string name())

- long curl.mime.data (long PartId, const string data(), [long datasize])

- long curl.mime.data_cb (long PartId, long sio_id, long datasize)

- long curl.mime.filedata (long PartId, const string local_filename())

- long curl.mime.filename (long PartId, const string remote_filename())

- long curl.mime.subparts (long PartId, long mime_handle)

- long curl.mime.type (long PartId, const string type())

- long curl.mime.headers (long PartId, long slist_id)

- long curl.mime.encoder (long PartId, const string encoding())

- void curl.mime.free (long mime_handle)

- long curl.setopt.mimepost(long mime_handle)

- CURLE_NOT_BUILT_IN

- CURLE_WEIRD_SERVER_REPLY

- CURLE_FTP_ACCEPT_FAILED

- CURLE_FTP_ACCEPT_TIMEOUT

- CURLE_HTTP2

- curl.setopt.accepttimeout_ms(aParameter)

- curl.setopt.address_scope(aParameter)

- curl.setopt.certinfo(aParameter)

- curl.setopt.expect_100_timeout_ms(aParameter)

- curl.setopt.gssapi_delegation(aParameter)

- curl.setopt.headeropt(aParameter)

- curl.setopt.keep_sending_on_error(aParameter)

- curl.setopt.path_as_is(aParameter)

- curl.setopt.pipewait(aParameter)

- curl.setopt.proxy_sslversion(aParameter)

- curl.setopt.proxy_ssl_options(aParameter)

- curl.setopt.proxy_ssl_verifyhost(aParameter)

- curl.setopt.proxy_ssl_verifypeer(aParameter)

- curl.setopt.rtsp_client_cseq(aParameter)

- curl.setopt.rtsp_request(aParameter)

- curl.setopt.rtsp_server_cseq(aParameter)

- curl.setopt.sasl_ir(aParameter)

- curl.setopt.socks5_auth(aParameter)

- curl.setopt.socks5_gssapi_nec(aParameter)

- curl.setopt.ssh_compression(aParameter)

- curl.setopt.ssl_enable_alpn(aParameter)

- curl.setopt.ssl_enable_npn(aParameter)

- curl.setopt.ssl_falsestart(aParameter)

- curl.setopt.ssl_options(aParameter)

- curl.setopt.ssl_verifystatus(aParameter)

- curl.setopt.stream_weight(aParameter)

- curl.setopt.suppress_connect_headers(aParameter)

- curl.setopt.tcp_fastopen(aParameter)

- curl.setopt.tcp_keepalive(aParameter)

- curl.setopt.tcp_keepidle(aParameter)

- curl.setopt.tcp_keepintvl(aParameter)

- curl.setopt.tftp_blksize(aParameter)

- curl.setopt.tftp_no_options(aParameter)

- curl.setopt.transfer_encoding(aParameter)

- curl.setopt.chunk_data(aParameter)

- curl.setopt.closesocketdata(aParameter)

- curl.setopt.debugdata(aParameter)

- curl.setopt.default_protocol(aParameter)

- curl.setopt.dns_interface(aParameter)

- curl.setopt.dns_local_ip4(aParameter)

- curl.setopt.dns_local_ip6(aParameter)

- curl.setopt.dns_servers(aParameter)

- curl.setopt.login_options(aParameter)

- curl.setopt.pinnedpublickey(aParameter)

- curl.setopt.pre_proxy(aParameter)

- curl.setopt.proxy_cainfo(aParameter)

- curl.setopt.proxy_capath(aParameter)

- curl.setopt.proxy_crlfile(aParameter)

- curl.setopt.proxy_keypasswd(aParameter)

- curl.setopt.proxy_pinnedpublickey(aParameter)

- curl.setopt.proxy_service_name(aParameter)

- curl.setopt.proxy_sslcert(aParameter)

- curl.setopt.proxy_sslcerttype(aParameter)

- curl.setopt.proxy_sslkey(aParameter)

- curl.setopt.proxy_sslkeytype(aParameter)

- curl.setopt.proxy_ssl_cipher_list(aParameter)

- curl.setopt.proxy_tlsauth_password(aParameter)

- curl.setopt.proxy_tlsauth_type(aParameter)

- curl.setopt.proxy_tlsauth_username(aParameter)

- curl.setopt.request_target(aParameter)

- curl.setopt.rtsp_session_id(aParameter)

- curl.setopt.rtsp_stream_uri(aParameter)

- curl.setopt.rtsp_transport(aParameter)

- curl.setopt.service_name(aParameter)

- curl.setopt.ssh_knownhosts(aParameter)

- curl.setopt.tlsauth_password(aParameter)

- curl.setopt.tlsauth_type(aParameter)

- curl.setopt.tlsauth_username(aParameter)

- curl.setopt.xoauth2_bearer(aParameter)

## Related topics
- [Tools Interface Version (TIV)](tiv_overview.md)
